{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    { "source": "/socket.io/(.*)", "destination": "/server.ts" },
    { "source": "/(.*)", "destination": "/server.ts" }
  ]
} 